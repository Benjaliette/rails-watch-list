  <div class="navbar-lewagon" data-controller="navbar" data-action="scroll@window->navbar#changeBackground">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <%= link_to root_path, class: "navbar-brand" do %>
          <%= image_tag "logo.svg" %>
        <% end %>

        <% if @list %>
          <h3>WATCHLIST</h3>
        <% end %>

        <%= link_to new_list_path, class:"btn-nav-add" do %>
          <div class="d-flex align-items-center justify-content-between">
            <div class="navbar-text mx-2">ADD LIST</div>
            <div class="navbar-text mx-2">+</div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
